// type alias -> short and sweet name for a type
type NS = number | string;

let chequeAmount : NS = 1000;

chequeAmount = 'Two thousand';

let cheques : NS[];

cheques = [ 1000, 'Two thousand', 3000, 'Four thousand'/*, true */ ];